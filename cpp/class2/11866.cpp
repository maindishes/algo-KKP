#include <iostream>
#include <vector>
#include <deque>
int main() {
    int n,k;
    std::cin >> n >> k;
    std::vector<int> my_vec;
    std::deque<int> iq;
    int f =0;
    int r = 0;
    for(int i=0; i<n; ++i) {
        my_vec.push_back(i+1);
    }
    int r = my_vec.size();
    for (int i =0; i<)

}

